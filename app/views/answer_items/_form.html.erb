<%= form_for(@answer_item) do |f| %>
  <% if @answer_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_item.errors.count, "error") %> prohibited this answer_item from being saved:</h2>

      <ul>
      <% @answer_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :sentence %><br />
    <%= f.text_field :sentence %>
  </div>
  <div class="field">
    <%= f.label :question_id %><br />
    <%= f.number_field :question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
